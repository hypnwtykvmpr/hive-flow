# @hive-flow/cli

Compatibility package for projects that still import `@hive-flow/cli`.

The canonical package name is now `hive-flow`. This package forwards the public
exports and legacy binaries to `hive-flow` so existing consumers can migrate
without a hard cutover.
