version: '3.8'

services:
  hive-flow-test:
    build:
      context: ../..
      dockerfile: tests/docker/Dockerfile.test
    container_name: hive-flow-test
    volumes:
      - test-data:/home/testuser/data
    environment:
      - NODE_ENV=test
      - CI=true
    command: sh -c "sleep infinity"

volumes:
  test-data:
